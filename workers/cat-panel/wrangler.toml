name = "cat-panel"
main = "src/index.js"
compatibility_date = "2026-09-21"
workers_dev = true

[vars]
SNI = "your-worker.your-subdomain.workers.dev"
WS_PATH = "/ws"
# Set these in Cloudflare secrets, never commit them:
# UUID, ADMIN_TOKEN, PROXY_IPS
