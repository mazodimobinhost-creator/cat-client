{
  "name": "cat-panel-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cat Panel Cloudflare Worker configuration and subscription endpoint",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
