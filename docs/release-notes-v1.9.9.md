# Cat Client v1.9.9 — پنل ۵.۱۴: منوی همبرگری بالا + رنگ‌های واقعا روشن‌تر

## چه چیزی تغییر کرد
- ☰ **دکمه‌های پایین رفتند بالا:** نوار پایین حذف شد؛ حالا دکمهٔ **سه‌خط (☰) بالای صفحه** همهٔ بخش‌ها (خانه، کانفیگ‌ها، اسکنر، کاربران، DNS، ابزارها، راهنما) را در یک منوی کشویی با آیکون + توضیح باز می‌کند. با انتخاب هر بخش، منو خودش بسته می‌شود؛ کلیک بیرون از منو هم می‌بندد. در دسکتاپ همان سایدبار مدیریتی سر جایش است و ☰ پنهان می‌شود.
- 🎨 **رنگ‌ها این‌بار واقعا عوض شد:** پس‌زمینه از سرمه‌ای تیره به **بنفشِ گرگ‌ومیش روشن‌تر** رفت، سطح کارت‌ها و فیلدها روشن‌تر و واضح‌تر شد، متن‌ها سفیدتر، خطوط پُررنگ‌تر، و اکسنت بنفشِ روشن‌تر (#c084fc) با سایه‌های نرم‌تر. تیرهٔ قبلی که «هیچی معلوم نبود» کنار رفت.
- 🖥️ جای خالی نوار پایین هم پر شد — محتوا تا پایین صفحه نفس می‌کشد.

## مسیر استفاده
1. `CatClient-V1.9.9-arm64-v8a.apk` (یا `universal`)؛ دانلود کند؟ پیشوند `https://ghproxy.net/`.
2. از داخل اپ **بروزرسانی پنل** بزن (پنل ۵.۱۴ — فقط رمز پنل پرسیده می‌شود).
3. پنل را باز کن: ☰ بالای صفحه → همهٔ بخش‌ها؛ اگر تم پیش‌فرض را می‌خواهی از 🎨 «بنفش» را انتخاب کن.

`SHA256SUMS` و `CatClient-Windows.zip` روی همین نسخه هستند.
